package katas

// NewCounter returns a function that, each time it's called, returns the
// next integer in sequence starting at 1 (1, 2, 3, ...). Two counters
// created by separate calls to NewCounter must not interfere with each
// other's count.
//
// TODO: implement NewCounter using a closure.
func NewCounter() func() int {
	panic("TODO: implement NewCounter")
}
