package katas

// ParallelForEach calls f(item) for every item in items, running each
// call in its own goroutine, and does not return until every call has
// completed.
//
// TODO: implement ParallelForEach using a goroutine per item + WaitGroup.
func ParallelForEach(items []int, f func(int)) {
	panic("TODO: implement ParallelForEach")
}
